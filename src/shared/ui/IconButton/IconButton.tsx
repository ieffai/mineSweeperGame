import { default as BaseIconButton } from '@mui/material/IconButton';
import type { IconButtonProps as BaseIconButtonProps } from '@mui/material/IconButton';

export type IconButtonProps = BaseIconButtonProps;
export const IconButton = BaseIconButton;
